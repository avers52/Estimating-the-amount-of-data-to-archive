<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Черновик алгоритма</title>
  <link rel="stylesheet" href="/css/compression_styles.css">
  <style>
    .btn-submit {
      width: 100%;
      padding: 12px;
      margin-top: 15px;
      margin-bottom: 20px;
      background-color: #007bff;
      color: #ffffff;
      border: none;
      border-radius: 8px;
      font-size: 15px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.2s ease;
    }
    .btn-submit:hover {
      background-color: #0056b3;
    }
    .input-field {
      width: 100%;
      box-sizing: border-box;
      margin-bottom: 10px;
      padding: 10px;
      border: 1px solid #dcdcdc;
      border-radius: 6px;
      font-size: 14px;
    }
    .input-field:read-only {
      background-color: #f5f5f5;
      color: #666666;
    }
    .draft-container {
      padding-bottom: 70px;
    }
  </style>
</head>
<body>
  <div class="mobile-frame">
    <div class="content-area">
      <div class="draft-container">
        <div class="section-header">Параметры алгоритма</div>
        <div class="section-subtitle">
          {{#if draft}}Черновой профиль (DRAFT){{else}}Новый алгоритм{{/if}}
        </div>

        {{#if draft}}
          <!-- Шаг 2: Черновик уже существует в БД -> Форма публикации карточки -->
          <form method="POST" action="/compression-algorithms/draft/publish/{{draft.id}}">
            <div class="form-card">
              <div class="form-card-title">1. Медиафайлы (MinIO)</div>
              <div class="media-preview-box">
                <img src="{{draft.image_url}}" alt="Превью" onerror="this.style.display='none'">
              </div>
              <input class="input-field" type="text" name="name" value="{{draft.name}}" readonly>
              <input class="input-field" type="text" name="image_url" value="{{draft.image_url}}" readonly>
              <input class="input-field" type="text" name="video_url" value="{{draft.video_url}}" readonly>
            </div>

            <div class="form-card">
              <div class="form-card-title">2. Параметры сжатия</div>
              <input class="input-field" type="number" step="0.01" name="compression_ratio" placeholder="Коэффициент сжатия (например, 3.8)" value="{{draft.compression_ratio}}" required>
              <input class="input-field" type="number" step="0.01" name="compression_speed_mbps" placeholder="Скорость сжатия МБ/с (например, 480)" value="{{draft.compression_speed_mbps}}" required>
              <textarea class="input-field" name="description" rows="3" placeholder="Краткое описание алгоритма" required>{{draft.description}}</textarea>
            </div>

            <button type="submit" class="btn-submit">Опубликовать</button>
          </form>

        {{else}}
          <!-- Шаг 1: Черновика еще нет -> Форма первичного создания черновика -->
          <form method="POST" action="/compression-algorithms/draft/create">
            <div class="form-card">
              <div class="form-card-title">1. Медиафайлы (MinIO)</div>
              <input class="input-field" type="text" name="name" placeholder="Название алгоритма" required>
              <input class="input-field" type="text" name="image_url" placeholder="URL или имя обложки (zstd.png)" required>
              <input class="input-field" type="text" name="video_url" placeholder="URL или имя видео (zstd.mp4)" required>
            </div>

            <button type="submit" class="btn-submit">Далее</button>
          </form>
        {{/if}}

      </div>
    </div>

    <nav class="tab-navigation">
      <a href="/compression-algorithms/feed" class="tab-link">Лента</a>
      <a href="/compression-algorithms/draft" class="tab-link active">Черновик</a>
      <a href="/compression-algorithms/catalog" class="tab-link">Плитка</a>
    </nav>
  </div>
</body>
</html>